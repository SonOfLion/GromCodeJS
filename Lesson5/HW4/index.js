function compareSums(a, b, c, d) {
    let from = a + b;
    let to = c + d;

    if (from > to) {
        return 'true';
    } else {
        return 'false';
    }

}

function sum(from, to) {
    let from = a + b;
    let to = c + d;
}