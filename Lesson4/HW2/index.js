let n = 50;

let result = 0;

for (let m = 1; m < n; m++) {
    if (n, m % 2 === 0 && n, m % 4 !== 0) {
        result = n + m;
    } else if (n, m % 3 === 0) {
        result = n - m;
    } else if (n, m % 4 === 0) {
        result = n * m;
    } else if (n, m % 5 === 0) {
        console.log(result);
    }

}