let m = 10;

let result = 0;

for (let n = 40; n > 10; n++) {
    if (n, m % 2 === 0 && n, m % 4 !== 0) {
        result = n + m;
    } else if (n, m % 3 === 0) {
        result = n - m;
    } else if (n, m % 4 === 0) {
        result = n * m;
    }


}